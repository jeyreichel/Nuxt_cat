{
  "version": 2,
  "builds": [
    { 
      "src": "*.js", 
      "use": "@vercel/node",
      "config": {
        "serverFiles": ["api/**", "middleware/**"]
      } 
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
